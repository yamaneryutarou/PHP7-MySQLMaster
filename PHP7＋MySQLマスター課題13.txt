13-1
	・送り主の会員検索
	・メッセージ一覧表
	・メッセージ受信状況確認
	
	
13-2
	・メッセージ内容
	・送信した会員
	・受信した会員
	・送信日時
	・タイトル欄
	
	
13-3
	・会員がログインしたらIDチェック
	・IDからメッセージが存在しているかをチェック
	・存在している場合は件数を表示する。
	・存在しない場合はお知らせしない。
	
	
13-4
	//メッセージ件数表示機能
	//引数:メッセージ件数
	function display_number_of_message(){
	if ($number == 0) {}  //0の場合何もしない
	else if ($number >= 1) {  //1以上の場合件数分表示
	print 'メッセージが'.$number.'件あります。'
	 }
	}